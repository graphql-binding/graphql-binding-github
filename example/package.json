{
  "scripts": {
    "start": "node src",
    "deploy": "now --public -e GITHUB_TOKEN && now alias && now rm --yes --safe graphql-binding-github"
  },
  "dependencies": {
    "graphql-binding-github": "0.2.1",
    "graphql-import": "0.1.8",
    "graphql-yoga": "0.5.6"
  },
  "now": {
    "alias": "graphql-binding-github"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
